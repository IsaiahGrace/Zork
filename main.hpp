#ifndef _MAIN_HPP_
#define _MAIN_HPP_

// Is this a good way to create a FSM in C++? It's how Verilog works, but maybe C++ will have a better way
/*
enum stateType {
  WAIT_FOR_COMMAND          = 0,
  CHECK_FOR_COMMAND_OVERIDE = 1,
  EXECUTE_COMMAND           = 2,
  CHECK_FOR_TRIGER_ACTIVATE = 3,
  EXECUTE_TRIGGER           = 4
  };*/

#endif
